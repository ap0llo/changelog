{
  "changelog": {

    "scopes": [ ],

    "footers": [ ],

    "markdown": {
      "preset": "default"
    },

    "tagPatterns": [
      "^(?<version>\\d+\\.\\d+\\.\\d+.*)",
      "^v(?<version>\\d+\\.\\d+\\.\\d+.*)"
    ],

    "outputPath": "changelog.md",

    "integrations": {

      "provider": "none",

      "github": {
        "accessToken": null
      },

      "gitlab": {
        "accessToken": null
      }
    },

    "versionRange": null,

    "currentVersion": null
  }
}
